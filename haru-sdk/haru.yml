# Just an planning example

class: de.shareui.harusdk.Main   # entry point class, see 4.2 — placeholder name, actual FQCN TBD
metadata: metadata.yml
source: src # compile all files in this folder

out: build/latest.dex

kotlinc: ">=" # placeholder for version
# javac:
# both are supported, the choice depends on the file format

include:
  - *.kt
  - *.java

libs:
  - path/to/jar/kotlin-stdlib.jar
