[package]
name = "haru"
version = "0.1.0"
edition = "2024"

[dependencies]
ratatui = "0.30"
clap = { version = "4.6", features = ["derive"] }
dirs = "6.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[target.'cfg(windows)'.dependencies]
walkdir = "2.5"
